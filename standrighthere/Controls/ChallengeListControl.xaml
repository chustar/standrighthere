<UserControl x:Class="standrighthere.Controls.ChallengeListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:utilities="clr-namespace:standrighthere.Utilities"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:transformer="clr-namespace:System.Windows.Controls;assembly=LayoutTransformer"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <phone:LongListSelector Grid.Row="1" x:Name="Challenges" ItemsSource="{Binding Challenges}" ItemRealized="LongListSelector_ItemRealized" SelectionChanged="Challenges_SelectionChanged">
            <phone:LongListSelector.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="12,10,0,16" Height="120" Background="LightBlue">
                        <Grid>
                            <Image Source="{Binding Image}" Height="120" Width="120" />
                            <StackPanel Background="Red" VerticalAlignment="Bottom">
                                <TextBlock Text="{Binding Distance}" Padding="2" FontSize="{StaticResource PhoneFontSizeMedium}" />
                            </StackPanel>
                        </Grid>
                        <Grid Margin="12,0,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="52" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Margin="0" Text="{Binding Title}" FontSize="{StaticResource PhoneFontSizeLarge}" TextTrimming="WordEllipsis" />
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="175" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <transformer:LayoutTransformer Width="30">
                                        <transformer:LayoutTransformer.LayoutTransform>
                                            <RotateTransform Angle="270"/>
                                        </transformer:LayoutTransformer.LayoutTransform>
                                        <Grid Background="Red">
                                            <TextBlock Text="solved" FontSize="{StaticResource PhoneFontSizeSmall}" HorizontalAlignment="Center" Padding="4" Margin="0,-3,0,0" />
                                        </Grid>
                                    </transformer:LayoutTransformer>
                                    <TextBlock Text="{Binding SolvedCount}" FontSize="{StaticResource PhoneFontSizeExtraExtraLarge}" Margin="12,-16,0,0" Width="120" />
                                </StackPanel>
                                <Grid Grid.Column="1" Margin="12,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30" />
                                        <ColumnDefinition Width="160" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <transformer:LayoutTransformer Grid.Row="0" Grid.RowSpan="2" Grid.Column="0">
                                        <transformer:LayoutTransformer.LayoutTransform>
                                            <RotateTransform Angle="270"/>
                                        </transformer:LayoutTransformer.LayoutTransform>
                                        <Grid Background="Red">
                                            <TextBlock Text="details" FontSize="{StaticResource PhoneFontSizeSmall}" HorizontalAlignment="Center" Padding="4" Margin="0,-3,0,0" />
                                        </Grid>
                                    </transformer:LayoutTransformer>
                                    <TextBlock Grid.Row="0" Grid.Column="1" DataContext="{Binding User}" Text="{Binding Username}" FontSize="{StaticResource PhoneFontSizeSmall}" Margin="10,4,0,0" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CreatedRelative}" FontSize="{StaticResource PhoneFontSizeSmall}" Margin="10,4,0,0" />
                                </Grid>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </phone:LongListSelector.ItemTemplate>
        </phone:LongListSelector>
    </Grid>
</UserControl>