<phone:PhoneApplicationPage
    x:Class="standrighthere.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:transformer="clr-namespace:System.Windows.Controls;assembly=LayoutTransformer"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    Loaded="Home_Loaded"
    shell:SystemTray.IsVisible="True">
    
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
            <phone:Panorama Title="stand right here">
            <phone:PanoramaItem Header="near me" Orientation="Horizontal">
                <StackPanel>
                    <ListBox Margin="12,0,0,0" ItemsSource="{Binding Challenges}" >
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <toolkit:WrapPanel Orientation="Horizontal" Width="412" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="173" Height="173" Margin="0,0,32,0">
                                    <Image Source="{Binding Image}" Width="173" Height="173" />
                                    <StackPanel Height="60" VerticalAlignment="Bottom">
                                        <StackPanel.Background>
                                            <SolidColorBrush Color="{StaticResource PhoneAccentColor}" Opacity="0.5" />
                                        </StackPanel.Background>
                                        <TextBlock Text="{Binding Title}" FontSize="{StaticResource PhoneFontSizeMedium}" VerticalAlignment="Bottom" HorizontalAlignment="Left" TextTrimming="WordEllipsis" Margin="12,0,0,0" Foreground="{StaticResource PhoneBackgroundBrush}" />
                                        <TextBlock Text="{Binding Distance}" FontSize="{StaticResource PhoneFontSizeSmall}"  Margin="12,0,0,0" Foreground="{StaticResource PhoneBackgroundBrush}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <HyperlinkButton Content="See All" NavigateUri="/ChallengeList.xaml" Style="{StaticResource HyperlinkStyle}" />
                </StackPanel>
            </phone:PanoramaItem>

            <phone:PanoramaItem Header="new">
                <Grid>
                    <Button Content="New Challenge" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Begin_PhotoChooserTask" />
                </Grid>
            </phone:PanoramaItem>
            
            <phone:PanoramaItem Header="dcz">
                <Grid>
                    <HyperlinkButton x:Name="Join" Content="Join" NavigateUri="/NewUser.xaml" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource HyperlinkStyle}" Visibility="Collapsed" />
                    <Grid x:Name="UserDetails">
                        <StackPanel Margin="12,0,0,0">
                            <TextBlock Text="{Binding UserCreatedRelative}" FontSize="{StaticResource PhoneFontSizeLarge}" Margin="0,-24,0,0" />
                            <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
                                <transformer:LayoutTransformer>
                                    <transformer:LayoutTransformer.LayoutTransform>
                                        <RotateTransform Angle="270"/>
                                    </transformer:LayoutTransformer.LayoutTransform>
                                    <Grid Background="Red" VerticalAlignment="Center">
                                        <TextBlock Text="submitted" FontSize="{StaticResource PhoneFontSizeLarge}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="12" />
                                    </Grid>
                                </transformer:LayoutTransformer>
                                <TextBlock Text="{Binding UserSubmittedCount}" FontSize="132" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="24,0,0,0" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,24,0,0">
                                <transformer:LayoutTransformer>
                                    <transformer:LayoutTransformer.LayoutTransform>
                                        <RotateTransform Angle="270"/>
                                    </transformer:LayoutTransformer.LayoutTransform>
                                    <Grid Background="Red">
                                        <TextBlock Text="solved" FontSize="{StaticResource PhoneFontSizeLarge}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="12 "/>
                                    </Grid>
                                </transformer:LayoutTransformer>
                                <Grid>
                                    <TextBlock Text="{Binding UserSolvedCount}" FontSize="132" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="24,0,0,0" />
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Grid>
            </phone:PanoramaItem>
        </phone:Panorama>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->

    </Grid>
</phone:PhoneApplicationPage>