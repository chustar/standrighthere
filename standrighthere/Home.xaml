<phone:PhoneApplicationPage
    x:Class="standrighthere.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:ViewModels="clr-namespace:standrighthere.ViewModels"
    xmlns:Controls="clr-namespace:standrighthere.Controls"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="False" Loaded="Home_Loaded">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
            <phone:Panorama Title="stand right here">
            <phone:PanoramaItem Header="near me" Orientation="Horizontal" Width="1000">
                <StackPanel>
                    <ListBox Margin="0,0,-12,0" ItemsSource="{Binding Challenges}" >
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <toolkit:WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.Template>
                            <ControlTemplate>
                                <ItemsPresenter />
                            </ControlTemplate>
                        </ListBox.Template>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Image Source="{Binding Image}" Width="173" Height="173" Stretch="Fill" Margin="12,0,0,0" />
                                    <TextBlock Text="{Binding Title}" Style="{StaticResource PhoneTextNormalStyle}" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <HyperlinkButton Content="See All" NavigateUri="/ChallengeList.xaml" HorizontalAlignment="Left" Margin="0,12,0,0"/>
                </StackPanel>
            </phone:PanoramaItem>

            <phone:PanoramaItem Header="new">
                <Grid>
                    <HyperlinkButton Content="New Challenge" NavigateUri="/NewChallenge.xaml" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,12,0,0"/>
                </Grid>
            </phone:PanoramaItem>
            
            <phone:PanoramaItem Header="me">
                <Grid>
                    <HyperlinkButton x:Name="Join" Content="Join" NavigateUri="/NewUser.xaml" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,12,0,0" Visibility="Collapsed" />
                    <Controls:UserDetailsControl x:Name="UserDetails" Visibility="Collapsed" />
                </Grid>
            </phone:PanoramaItem>
        </phone:Panorama>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->

    </Grid>
</phone:PhoneApplicationPage>